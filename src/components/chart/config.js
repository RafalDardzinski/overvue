import colors from '@/config/colors.scss';

const config = {
  datasets: {
    defaultBackgroundColors: [
      colors.mono1,
      colors.mono2,
      colors.mono3,
      colors.mono4,
      colors.mono5
    ]
  }
};